
{
  "name": "swensi-nakonde-terminal",
  "version": "1.1.0",
  "description": "Swensi Nakonde - Trade & Transport Logistics Link",
  "main": "server.js",
  "license": "MIT",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "build": "rm -rf build && mkdir build && cp index.html manifest.json sw.js metadata.json build/ && cp *.tsx build/ 2>/dev/null || true && cp *.ts build/ 2>/dev/null || true && cp -r components build/ 2>/dev/null || true"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "engines": {
    "node": "20.x"
  }
}
